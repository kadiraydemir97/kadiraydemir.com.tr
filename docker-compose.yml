version: '3.8'

services:
  web:
    build: .
    ports:
      - "3000:80"
    restart: always
    container_name: kadiraydemir-web
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
